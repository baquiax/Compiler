class Program {
	int value;
	int next;
	int tintin[5], rinrin, _foo[4];
	boolean ran, _fa_a[4],tang[5];
	int var, les5, jojojo, _7_5_4;
	boolean var2, les56, joja, _1_5_4;
	int a;
	int b;
	
	int create_list(int v, boolean alex) {
		int value;
		int value;		
		boolean new_element; 
		value = 'a';
		new_element = 5;
		value-=5;
		value+=6;
		
		return new_element;
	}
	
	boolean insert(int v) {
		boolean new_element;
		new_element = old_element;
		continue;
		return new_element;
	}

	boolean search(int v) {
		var = hola;
		if (value == v) {
			return true;
			continue;
		}				 		

		for expresion = 4, 5 {
			int hola;
			hola=10;
			for expresion=4, 5 {
				int hola;
			}
		}
	}

	boolean length() {
		if (next == null) {
			return 1;
		} else {
			break;
			return 1 + lock;
		} 
	}

	int main() {
		int x, y, z;
		x = a;
		y = b+a;
		z = gcd(x, y);
		// print_int is part of the standard input-output library
		print_int(z);

		if (a[4]==0) {
			return;
		}
	}

	// function that computes the greatest common divisor
	int gcd(int a, int b) {
		if ((b == 0) || (c==40)) {
			break;
			return(a); 
		} else {
			return(gcd(b, a % b)); 
		}
	}

	int f() {
		return 2; 
	}

	int g() { 
		return 3;
	}

	int f(int z) {
		if (z > 0) {
			int z;
			z = x * 2;
			return z; // line 1
		}
		
		return z; // line 2
	}

	void hola() {
		int val;
		var = '';
		val= 2  + 3   + 3;
		val = adios(3 ,2 ,3);
		if (hola==SS) {
			return;
		}
	}

	void adios(int value, boolean val, boolean lala) {
		int val;
		boolean val2;
		int val3;
		val = value;
		val2 = lala;
	}
}
